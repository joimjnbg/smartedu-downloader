## SmartEdu下载器 v1.4.0

### 主要更新

#### 下载引擎全面升级（高可用 + 高性能）

- **并发下载**：同时下载多个文件（可在界面选择 1/2/4/8 路并发），整批下载速度提升数倍
- **断点续传**：下载中断后重新下载自动从断点继续（HTTP Range），无需重头开始；已下载完整的文件自动跳过
- **失败自动重试**：网络波动、超时、服务器 5xx/429 时按指数退避自动重试；受限资源 401/403 自动携带 Token 重试
- **可随时取消**：下载过程中可一键取消整个批次，取消后的进度下次可续传
- **磁盘空间预检**：开始下载前检查目标磁盘剩余空间，不足时给出警告
- **实时进度**：总进度按字节精确计算，显示当前文件与实时下载速度

#### 工程重构

- 抽出 `net.js`（HTTP 客户端层）与 `downloader.js`（下载引擎），可脱离 Electron 单独测试
- 新增 `tdd-downloader.js` 测试套件（19 项）：本地 HTTP 服务模拟断线、超时、重试、续传、取消、并发上限
- 新增 `npm test` 命令，一键运行全部测试（逻辑层 67 项 + 下载引擎 19 项）
- 修复多个问题：下载流只计数不写盘、同 URL 文件并发冲突、取消后队列挂起、Token 对话框外链失效
- 界面增加并发数选择与取消按钮

### 功能保持

- 教材、课件、作业、课程活动、实验课、精品课、专题课程等资源下载
- 自动发现所有资源分类，无需硬编码白名单
- 课件/教案 PDF 转换回退自动标注 `[PDF转换]`
- 图片/音频等格式支持

> 视频下载仍然不可用：平台使用 AES-128 加密 + 华为 WAF JS Challenge，Electron 无法可靠绕过，见 README 说明。
