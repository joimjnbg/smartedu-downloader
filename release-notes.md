## SmartEdu下载器 v1.5.0

### 新功能：教材目录批量下载

- **整册教材批量下载**：粘贴教材栏目（`tchMaterial`）目录链接，自动按「学段 → 学科 → 版本 → 年级 → 册次」展开全部分类，勾选即可批量下载
- **覆盖全部教材**：平台全量教材元数据（约 3600 本），首次加载后本地缓存秒开
- **自动目录结构**：下载后按「学段/学科/版本/年级/册次/书名.pdf」自动组织保存目录
- 新增 `catalog.js` 目录解析模块与 `tdd-catalog.js` 测试（16 项），`npm test` 总计 102 项全部通过

### 其他

- 打包配置已包含新增模块，构建后自动校验 `app.asar` 完整性（防止漏包）

### 历史更新

#### v1.4.0

- **下载引擎升级**：并发下载（1/2/4/8 路）、断点续传（HTTP Range）、失败自动重试（指数退避）、一键取消、磁盘空间预检、字节级实时进度
- **工程重构**：抽出 `net.js` / `downloader.js` 可独立测试；新增 `tdd-downloader.js`（19 项）；修复流不写盘、并发冲突、取消挂起等缺陷

### 功能保持

- 教材、课件、作业、课程活动、实验课、精品课、专题课程等资源下载
- 自动发现所有资源分类，无需硬编码白名单
- 课件/教案 PDF 转换回退自动标注 `[PDF转换]`
- 图片/音频等格式支持

> 视频下载仍然不可用：平台使用 AES-128 加密 + 华为 WAF JS Challenge，Electron 无法可靠绕过，见 README 说明。
