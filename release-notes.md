## SmartEdu下载器 v1.7.3

### 彻底修复 HTTP 400 [OSS InvalidArgument]

**根因**：私有 CDN（`r1-ndr-private`）的资源下载，客户端发送了 `Authorization: Bearer <token>` 头。该头在通过 CDN 校验后会被**随机透传给上游 OSS**，OSS 试图把它当作自己的签名解析 → 间歇性返回 `HTTP 400 InvalidArgument`（表现为"总是有几个文件 400，重试偶尔成功"）。已用你提供的文件（盲校物理八年级上册）完整复现并验证。

**修复**：
- 私有 CDN 资源改用 **`X-ND-AUTH` 头鉴权（不带 Authorization）**——私有 CDN 只认该头，实测 12/12 次全部成功
- 其他平台 API 请求不受影响（仍用 Authorization）
- 断点续传、自动重试逻辑不变，与修复前的行为完全兼容

**验证**：你失败的文件（47966263 盲校物理）修复后完整下载成功（15.6MB 续传完成）。

### 历史

- v1.7.2：过滤转码分页图（OSS 400）+ 400 自动重试 + 日志显示 OSS 错误码
- v1.7.1：搜索结果修复 + 原始错误日志与网页核查
- v1.7.0：教材关键字搜索（书名 / 学科 / 版本 / 年级 / 出版社）
- v1.6.0：失败文件一键重试 + 友好错误诊断 + Token 引导
- v1.5.2：修复半选复选框误全选 + 下载前确认
- v1.5.1：修复批量下载 ENOENT（自动创建子目录）
- v1.5.0：教材目录批量下载

> 视频下载仍然不可用：平台使用 AES-128 加密 + 华为 WAF JS Challenge，Electron 无法可靠绕过，见 README 说明。
