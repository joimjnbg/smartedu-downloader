## SmartEdu下载器 v1.7.2

### 修复 HTTP 400 下载失败

**根因**：教材详情中包含「转码分页图」（`transcode/image/*.jpg`，即电子书逐页扫描图）。这些图片存放在私有 OSS 桶，服务端会把我们的 `Authorization: Bearer` 头当作 OSS 签名解析并拒绝——**无论 Token 是否正确，都会返回 `HTTP 400 InvalidArgument`**，永远无法下载。

**修复**：
- 解析时自动过滤转码分页图（folder / thumbnail 条目），下载列表不再出现必然失败的 400 文件（不影响主文件 PDF / PPT 等）
- HTTP 400 现在**自动重试**（平台 CDN 偶发的瞬时 400 可自愈）
- 错误消息与复制日志中显示**服务端原始错误码**（如 `HTTP 400 [OSS InvalidArgument]`），排查更精准

**注意**：你给的示例链接（英语 选择性必修二 bfb3f0ef）实测下载完全正常（33MB PDF），400 正是来自其配套的分页图。更新后重新解析一次列表即可。若仍有 400，点 📋 复制日志发给我们，可直接定位。

### 历史

- v1.7.1：搜索结果修复 + 原始错误日志与网页核查
- v1.7.0：教材关键字搜索（书名 / 学科 / 版本 / 年级 / 出版社）
- v1.6.0：失败文件一键重试 + 友好错误诊断 + Token 引导
- v1.5.2：修复半选复选框误全选 + 下载前确认
- v1.5.1：修复批量下载 ENOENT（自动创建子目录）
- v1.5.0：教材目录批量下载

> 视频下载仍然不可用：平台使用 AES-128 加密 + 华为 WAF JS Challenge，Electron 无法可靠绕过，见 README 说明。
